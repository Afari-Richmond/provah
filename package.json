{
  "name": "provah",
  "private": true,
  "description": "Repo-root tooling only (commit linting). App code lives in /mobile-app and /server.",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.2.2",
    "@commitlint/config-conventional": "^21.2.2",
    "husky": "^9.1.7"
  }
}
