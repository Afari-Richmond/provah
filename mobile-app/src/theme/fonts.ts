// No custom font family chosen yet (see context/ui-context.md); system
// default with bold weights matches the reference's geometric-sans headline
// style closely enough for now.
export const fontWeight = {
  regular: "400" as const,
  medium: "600" as const,
  bold: "700" as const,
  heavy: "800" as const,
};
